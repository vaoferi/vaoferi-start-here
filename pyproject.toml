[project]
name = "vaoferi-start-here"
version = "0.2.4"
description = "Canonical Vaoferi AI-agent contract, skills, sync and verification tooling"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
